function preco(n1) {
    if (n1 > 20) {
        return true
    } else {
        return false
    }
}
let totalpreco = preco(40)
console.log("Este é o preço: " + totalpreco)

let precototal = 20
    if (precototal <= 18) {
        console.log("O preço está barato: " + precototal)
    } else {
        console.log("O preço esta caro: " + precototal)
}